pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "org.openapi.generator" version "7.0.0-beta"
    }
}

rootProject.name = 'contract-first-api-example'

include 'api'
include 'service'
include 'client'

