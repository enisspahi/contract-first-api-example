pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "org.openapi.generator" version "7.1.0"
    }
}

rootProject.name = 'contract-first-api-example'

include 'api'
include 'client'
include 'service'

